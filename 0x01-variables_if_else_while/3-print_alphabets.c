#include <stdio.h>
/**
 *  * main block
 *   * Description:  prints the alphabet in lowercase,
 *    * then in uppercase.
{
	char c = 'a';

	while (c <= 'z')
	{
		putchar(c);
		c++;
	}

	c = 'A';

	while (c <= 'Z')
	{
		putchar(c);
		c++;
	}

	putchar('\n');
	return (0);
}
