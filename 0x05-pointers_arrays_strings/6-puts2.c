#include "main.h"
/**
 * puts2 - Prints every line
 * of a string
 * @s: Points to string
 * Return: return void
 */

void puts2(char *str)
{
    int i;

    for (i = 0; i <= 0; i++)
    {
        _putchar(str[i]);
        _putchar('\n');

    }
}
